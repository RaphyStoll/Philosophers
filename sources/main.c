#include "philo.h"

bool	parse_arg(int argc, char **argv)
{
	if (argc != 5 || argc != 6 )
		return (false);
	if (argv[1] )
}

int	main(int argc, char **argv)
{
	if (!parse_arg(argc, argv))
		return (1);
}